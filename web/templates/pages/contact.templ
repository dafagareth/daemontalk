package pages

import (
	"daemontalk/internal/i18n"
	"daemontalk/web/templates/components"
	"daemontalk/web/templates/shared"
)

templ ContactPage(ui i18n.UI, lang string) {
	<section class="container-page py-8 sm:py-12 min-h-[75vh]">
		<div class="mb-8 pb-4 border-b border-border/20">
			<a
				href={ templ.SafeURL(shared.Prefix(lang) + "/") }
				class="inline-flex items-center gap-1.5 text-xs text-muted hover:text-text transition-colors mb-4 group font-medium"
			>
				@components.IconArrowLeft("w-3.5 h-3.5")
				<span>Back to Dispatches</span>
			</a>
			<h1 class="display text-2xl sm:text-4xl font-black text-text tracking-tight uppercase">{ ui.Link_Contact }</h1>
			<p class="text-sm text-muted font-sans mt-2 max-w-lg">
				Send me a message — I read everything and reply when I can.
			</p>
		</div>
		<div class="max-w-lg font-sans">
			<form
				hx-post={ string(templ.SafeURL(shared.Prefix(lang) + "/contact")) }
				hx-target="#contact-result"
				hx-swap="innerHTML"
				class="flex flex-col gap-4"
			>
				<!-- ponytail: honeypot hidden from users, catches bots that fill all fields -->
				<input type="text" name="website" class="hidden" tabindex="-1" autocomplete="off"/>
				<div class="flex flex-col gap-1">
					<label for="contact-name" class="text-xs font-semibold text-muted uppercase tracking-wider">{ ui.Contact_NameLabel }</label>
					<input
						id="contact-name"
						type="text"
						name="name"
						required
						class="bg-surface border border-border px-3 py-2.5 text-sm text-text placeholder:text-muted focus:outline-none focus:border-text transition-colors"
						placeholder="Your name"
					/>
				</div>
				<div class="flex flex-col gap-1">
					<label for="contact-email" class="text-xs font-semibold text-muted uppercase tracking-wider">{ ui.Contact_EmailLabel }</label>
					<input
						id="contact-email"
						type="email"
						name="email"
						required
						class="bg-surface border border-border px-3 py-2.5 text-sm text-text placeholder:text-muted focus:outline-none focus:border-text transition-colors"
						placeholder="you@example.com"
					/>
				</div>
				<div class="flex flex-col gap-1">
					<label for="contact-message" class="text-xs font-semibold text-muted uppercase tracking-wider">{ ui.Contact_MessageLabel }</label>
					<textarea
						id="contact-message"
						name="message"
						required
						rows="6"
						class="bg-surface border border-border px-3 py-2.5 text-sm text-text placeholder:text-muted focus:outline-none focus:border-text transition-colors resize-y"
						placeholder="What's on your mind?"
					></textarea>
				</div>
				<div id="contact-result" class="text-sm font-sans min-h-[1.5rem]"></div>
				<button
					type="submit"
					class="self-start px-5 py-2.5 bg-text text-bg text-xs font-bold uppercase tracking-wider hover:opacity-80 transition-opacity cursor-pointer"
				>
					{ ui.Contact_Submit }
				</button>
			</form>
		</div>
	</section>
}
