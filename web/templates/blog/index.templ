package blog

import (
	"daemontalk/web/templates/components"
	"daemontalk/web/templates/shared"
	"fmt"

	"daemontalk/internal/i18n"
	"daemontalk/internal/post"
	"daemontalk/web/templates/portal"
)

templ BlogIndex(ui i18n.UI, allPosts []post.Post, posts []post.Post, lang string, page int, totalPages int, viewCounts map[string]int, tagCounts map[string]int, currentTag string) {
	<section class="container-page pt-4 sm:pt-6 pb-10 sm:pb-14 transition-all">
		<h1 class="sr-only">{ ui.Blog_Title }</h1>
		if len(allPosts) == 0 {
			<p class="text-sm text-muted py-12 text-center">{ ui.Blog_NoPosts }</p>
		} else {
			<p id="blog-noresults" class="hidden text-sm text-muted mb-8 text-center py-8">{ ui.Blog_NoResults }</p>
			if currentTag == "" {
				@portal.PortalNewsLayout(ui, allPosts, tagCounts, lang, viewCounts)
			} else {
				@portal.TagPortal(ui, currentTag, allPosts, lang, viewCounts)
			}
			<script src="/static/js/blog_index.js"></script>
		}
	</section>
}

templ TagPage(ui i18n.UI, tag string, allPosts []post.Post, lang string, viewCounts map[string]int) {
	<section class="container-page py-6 sm:py-10 transition-all">
		@portal.TagPortal(ui, tag, allPosts, lang, viewCounts)
	</section>
}

templ blogCardItem(ui i18n.UI, p post.Post, lang string, viewCounts map[string]int, hideTag bool) {
	<article class="blog-item group flex flex-row sm:flex-col gap-4 sm:gap-0 min-w-0 items-start sm:items-stretch h-full" data-search={ searchIndex(p) }>
		<div class="relative aspect-[16/9] w-24 sm:w-full overflow-hidden rounded-none shrink-0 sm:mb-3 bg-surface">
			@components.PostThumbnail(p, shared.Prefix(lang)+"/blog/"+p.Slug)
		</div>
		<div class="flex-1 min-w-0 flex flex-col justify-center sm:justify-start h-full">
			<a href={ templ.SafeURL(shared.Prefix(lang) + "/blog/" + p.Slug) } data-slug={ p.Slug } class="block group mb-1 sm:mb-2 sm:mt-1">
				<h3 class="text-base sm:text-xl font-black text-text group-hover:text-link transition-colors line-clamp-3 leading-snug break-words">
					{ p.Title }
				</h3>
			</a>
			<div class="flex flex-wrap items-center gap-1 sm:gap-2 mb-1 sm:mb-2 text-xs font-sans text-muted uppercase tracking-wider">
				<time datetime={ p.Date.Format("2006-01-02") }>{ shared.FmtDate(p, lang) }</time>
				<span class="hidden sm:inline">·</span>
				<span class="hidden sm:inline">{ fmt.Sprintf("%d %s", p.ReadTime, ui.Blog_MinRead) }</span>
			</div>
			if p.Description != "" {
				<p class="hidden sm:line-clamp-2 text-sm text-muted  leading-relaxed mb-3.5 break-words">
					{ p.Description }
				</p>
			}
			if !hideTag && len(p.Tags) > 0 {
				<div class="hidden sm:flex flex-wrap gap-2 mt-auto pt-1">
					<a href={ templ.SafeURL(shared.Prefix(lang) + "/blog/tag/" + p.Tags[0]) } class="text-[10px] font-bold text-accent uppercase tracking-wider hover:underline">{ p.Tags[0] }</a>
				</div>
			}
		</div>
	</article>
}

templ BlogPostItems(ui i18n.UI, posts []post.Post, lang string, viewCounts map[string]int, nextPage int, hasMore bool, currentTag string) {
	for _, p := range posts {
		@blogCardItem(ui, p, lang, viewCounts, currentTag != "")
	}
	<div id="load-more-wrap" class="mt-12 mb-8 text-center" hx-swap-oob="true">
		if hasMore {
			<button
				hx-get={ fmt.Sprintf("/blog/posts?page=%d&lang=%s&tag=%s", nextPage, lang, currentTag) }
				hx-trigger="click"
				hx-target="#blog-grid"
				hx-swap="beforeend"
				class="px-6 py-2.5 border-2 border-border text-xs font-sans font-bold uppercase tracking-wider text-text hover:bg-hover transition-colors cursor-pointer inline-flex items-center justify-center min-w-[200px]"
			>
				LOAD MORE
			</button>
		}
	</div>
}
